# AUTO-GENERATED by scripts/generate_uc_banners.py — do not edit by hand.
procaenet-flood-segmentation:
  alt: "End-to-end pipeline diagram: Sentinel-2 RGB (visible bands) + Sentinel-2 NIR (water-sensitive) → Dual encoders (self-attention per branch) → Cross-attention (fused at every skip scale) → Flood extent masks (zero-shot sensor transfer)"
mining-footprint-segmentation:
  alt: "End-to-end pipeline diagram: Sentinel-2 optical + Sentinel-1 SAR + DEM terrain → Stream encoders (Prithvi FM (NASA/IBM)) → Cross-modal fusion (multi-year change stack) → Footprint maps (active vs rehabilitated)"
aquaculture-pond-detection:
  alt: "End-to-end pipeline diagram: Sentinel-2 stack (multi-temporal revisits) → Spectral indices (NDWI · MNDWI · AWEI) → Geometry filters (man-made pond shapes) → Expansion alerts (Indonesia + Vietnam coasts)"
flood-urban-resilience:
  alt: "End-to-end pipeline diagram: ProCANet flood masks + NDWI flood extent + Policy documents → Mixed-methods analysis (satellite + policy evidence) → Retention-pond KPIs (evidence for city planners)"
multilingual-vlm-crossmodal-conflict:
  alt: "End-to-end pipeline diagram: Conflict benchmark (EN · HI · TE · multi-domain) → Open VLM cohort (multilingual evaluation) → Linear probes (residual-stream readout) → Cross-lingual steering (text-override eliminated)"
heron-hijack-self-probe:
  alt: "End-to-end pipeline diagram: Clean + attacked + Benign-prefix control + Held-out family → Hidden-state capture (read-only, single forward pass) → Control-aware probe (logistic regression, one layer) → Conformal FPR gate (flag above calibrated threshold)"
sea-vl-benchmark:
  alt: "End-to-end pipeline diagram: Community annotators (across SEA countries) → Dedup + HITL review (pHash · CLIP · SigLIP) → SEA-VL dataset (culturally grounded VQA) → Frontier audit (GPT-4V · Gemini · Claude)"
gg-ez-regional-adaptation:
  alt: "End-to-end pipeline diagram: SEA visual corpus (curated regional) + SDXL base weights (general-purpose) → Diffusion fine-tune (cultural adaptation arm) → Linear weight merge (regional ↔ global blend) → Adapted model (cultural fidelity improved)"
commonlid-language-identification:
  alt: "End-to-end pipeline diagram: CommonCrawl text (code-switched · romanized) → Production LID stack (fastText · GlotLID · OpenLID) → Stratified eval (noise regimes isolated) → Failure map (guidance for LLM corpora)"
share-of-voice-forecasting:
  alt: "End-to-end pipeline diagram: SimilarWeb + Traackr (competitive intel) + Client media data (BigQuery · dbt) → XGBoost (SHAP-validated forecasts) → Split conformal (distribution-free intervals) → Planning dashboards (Streamlit · APAC markets)"
biometric-authentication-credit-scoring:
  alt: "End-to-end pipeline diagram: Login surge traffic (major bank clients) → MobileNet · OpenVINO (CPU-optimised runtime) → Verification API (sync + async load-tested) → Credit scorecard (points-based · explainable)"
fraud-detection-pipeline:
  alt: "End-to-end pipeline diagram: Kafka stream (Redis online features) → Rule pre-filters (velocity · geo · device) → XGBoost + graph (layered anomaly detectors) → Case routing (drift-monitored scores)"
municipal-waste-forecasting:
  alt: "End-to-end pipeline diagram: JAKI · Qlue reports (citywide citizen data) → Prophet ensemble (vs ARIMA · SARIMA) → DiD analysis (plastic-bag ban rollout) → Route planning (Tableau · per district)"
demand-forecasting-consulting:
  alt: "End-to-end pipeline diagram: Data triage (quality audit · BigQuery) → XGBoost · Prophet (fit per problem) → Client validation (sign-off vs intuition) → Documented handoff (model card · retraining)"
hakktaxi-ride-share:
  alt: "End-to-end pipeline diagram: Trip logs + Weather feed + POI density → H3 hex features (temporal + spatial) → XGBoost surge (per-cell demand) → Demand heatmap (driver positioning)"
telehealthmonitor-edge-ai:
  alt: "End-to-end pipeline diagram: Camera feed (chest-region video) → Pose + optical flow (respiratory-rate estimate) → On-device INT8 (ARM CPU · FP32→INT8) → 2G vitals uplink (vital signs only · no video)"
community-ivr-voice-ai:
  alt: "End-to-end pipeline diagram: Feature-phone call (PSTN · 2G · offline) → Indonesian ASR (dialect variants) → Intent + KG (curated health & aid info) → TTS response (DTMF-familiar UX)"
plastic-bag-ban-causal-analysis:
  alt: "End-to-end pipeline diagram: Citizen complaints (JAKI · Qlue platforms) → Text classification (waste-type taxonomy) → DiD estimation (mobility + season controls) → Causal verdict (fewer plastic complaints)"
aicity-qwen-vl:
  alt: "End-to-end pipeline diagram: Traffic video QA (challenge corpus) → Dataset re-pack (per-step memory cut) → LoRA adapters (Qwen2.5-VL-3B) → ZeRO-2 training (on a commodity V100)"
vln-conformal-prediction:
  alt: "End-to-end pipeline diagram: VLN-DUET + VLN-HAMT + Recurrent VLN-BERT → Confidence rescaling (divide by residual confidence) → Episode-max calibration (whole-trajectory coverage proof) → Help-seeking policy (operator queried on flagged steps)"
llm-d-inference-scheduler:
  alt: "End-to-end pipeline diagram: Inference requests (Gateway API ingress) → Envoy ext-proc (request interception) → KV-cache routing (P/D disaggregation) → vLLM backends (Kubernetes-native)"
